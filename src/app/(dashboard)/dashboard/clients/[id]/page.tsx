export default function ClientDetailPage({ params }: { params: Promise<{ id: string }> }) {
  return (
    <div>
      <h1 className="text-2xl font-bold text-foreground">Detail Client</h1>
      <p className="mt-2 text-muted-foreground">Detail client akan diimplementasikan di Fase 4.</p>
    </div>
  );
}
